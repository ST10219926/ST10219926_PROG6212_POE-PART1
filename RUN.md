Quick run instructions (inside ContractMonthlyClaimSystem folder):

1. dotnet restore
2. dotnet tool install --global dotnet-ef   (if you don't have dotnet-ef)
3. dotnet ef migrations add Initial
4. dotnet ef database update
5. dotnet run
